import React from "react";
import { Box, Typography } from "@mui/material";

const SignUpForm = () => {
  return (
    <Box>
      <Typography variant="h6">Sign up for a free account</Typography>
    </Box>
  );
};

export default SignUpForm;
